# Copiar para .env no Render (ou definir nas variáveis de ambiente)
# --- Necessário para entrar na malha Tailscale ---
TAILSCALE_AUTHKEY=tskey-auth-XXXXXXXXX

# --- Opcional ---
# PORT já é definida pelo Render (10000)
# Resolução: usar IP do container (só diagnóstico)